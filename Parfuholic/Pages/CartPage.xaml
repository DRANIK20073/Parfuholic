<Page x:Class="Parfuholic.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:models="clr-namespace:Parfuholic.Models"
      Background="#FFF5F5F5">

    <Page.Resources>

        <!-- Стиль TextBox как в ProfileDataPage -->
        <Style x:Key="ProfileTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <!-- Стиль placeholder -->
        <Style x:Key="ProfilePlaceholder" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>

    </Page.Resources>


    <Grid Margin="60,40,60,40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- КНОПКА НАЗАД -->
        <Button x:Name="BackButton"
                Content="←"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Width="40"
                Height="40"
                FontWeight="Bold"
                FontSize="20"
                Background="Black"
                Foreground="White"
                BorderBrush="Black"
                BorderThickness="2"
                Click="BackButton_Click"
                Grid.Column="0"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0">
                        <ContentPresenter x:Name="contentPresenter"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="White"/>
                            <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="Black"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                            <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="Black"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- ================= ЛЕВАЯ ЧАСТЬ — ТОВАРЫ ================= -->
        <StackPanel Grid.Column="0" Margin="0,50,0,0">

            <TextBlock Text="КОРЗИНА"
                       FontSize="32"
                       FontWeight="Bold"
                       Margin="0,0,0,30"/>

            <!-- Сообщение если корзина пуста -->
            <TextBlock x:Name="EmptyCartMessage"
                       Text="Ваша корзина пуста"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="Gray"
                       Visibility="Collapsed"/>

            <ItemsControl ItemsSource="{Binding CartItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:CartItem}">
                        <Border Background="White"
                                BorderBrush="#FFE0E0E0"
                                BorderThickness="1"
                                Padding="20"
                                Margin="0,0,0,15"
                                CornerRadius="0">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Фото -->
                                <Border Width="160"
                                        Height="160"
                                        Background="#FFF0F0F0"
                                        Padding="0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                    <Image Source="{Binding Perfume.Image}"
                                           Stretch="UniformToFill"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Border>


                                <!-- Инфо -->
                                <StackPanel Grid.Column="1" Margin="20,0">
                                    <TextBlock Text="ПАРФЮМЕРНАЯ ВОДА"
                                               Foreground="Gray"
                                               FontSize="12"
                                               Margin="0,0,0,5"/>

                                    <TextBlock Text="{Binding Perfume.Brand}"
                                               FontSize="14"
                                               FontWeight="SemiBold"/>

                                    <TextBlock Text="{Binding Perfume.Name}"
                                               FontSize="18"
                                               FontWeight="Bold"
                                               Margin="0,2,0,5"/>

                                    <TextBlock Text="{Binding Perfume.Volume}"
                                               Foreground="Gray"
                                               FontSize="14"
                                               Margin="0,0,0,10"/>

                                    <!-- Цены -->
                                    <Grid Margin="0,10,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Новая цена -->
                                        <TextBlock Grid.Column="0"
                                           Text="{Binding Price, StringFormat='{}{0:F2} BYN'}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Foreground="#000000"
                                           VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Perfume.IsDiscount}" Value="True">
                                                            <Setter Property="Foreground" Value="#000000"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <!-- Старая цена-->
                                        <TextBlock Grid.Column="2"
                                           Text="{Binding Perfume.Price, StringFormat='{}{0:F2} BYN'}"
                                           Foreground="Gray"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           TextDecorations="Strikethrough"
                                           VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Perfume.IsDiscount}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </StackPanel>

                                <!-- Управление количеством -->
                                <StackPanel Grid.Column="2"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Right">
                                    <StackPanel Orientation="Horizontal"
                                                Margin="0,0,0,10">
                                        <Button Content="-"
                                                Width="30"
                                                Height="30"
                                                Background="Black"
                                                Foreground="White"
                                                FontWeight="Bold"
                                                FontSize="14"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Tag="{Binding}"
                                                Click="DecreaseQuantity_Click">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#333333"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <TextBlock Text="{Binding Quantity}"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   Width="40"
                                                   TextAlignment="Center"
                                                   VerticalAlignment="Center"/>

                                        <Button Content="+"
                                                Width="30"
                                                Height="30"
                                                Background="Black"
                                                Foreground="White"
                                                FontWeight="Bold"
                                                FontSize="14"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Tag="{Binding}"
                                                Click="IncreaseQuantity_Click">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#333333"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </StackPanel>

                                    <Button Content="УДАЛИТЬ"
                                            Width="100"
                                            Height="30"
                                            Background="Transparent"
                                            Foreground="Gray"
                                            FontSize="12"
                                            BorderBrush="Gray"
                                            BorderThickness="1"
                                            Cursor="Hand"
                                            Tag="{Binding}"
                                            Click="RemoveItem_Click">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F5F5F5"/>
                                                        <Setter Property="Foreground" Value="Black"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- ================= ПРАВАЯ ЧАСТЬ — ОФОРМЛЕНИЕ ================= -->
        <StackPanel Grid.Column="2" Margin="0,50,0,0">

            <TextBlock Text="ОФОРМЛЕНИЕ ЗАКАЗА"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="0,0,0,25"/>

            <!-- Телефон -->
            <Grid Margin="0,0,0,20">
                <TextBox x:Name="PhoneBox" Style="{StaticResource ProfileTextBox}" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Телефон">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource ProfilePlaceholder}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=PhoneBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Email -->
            <Grid Margin="0,0,0,20">
                <TextBox x:Name="EmailBox" Style="{StaticResource ProfileTextBox}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Email">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource ProfilePlaceholder}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=EmailBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Имя -->
            <Grid Margin="0,0,0,20">
                <TextBox x:Name="FirstNameBox" Style="{StaticResource ProfileTextBox}" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Имя">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource ProfilePlaceholder}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=FirstNameBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Фамилия -->
            <Grid Margin="0,0,0,20">
                <TextBox x:Name="LastNameBox" Style="{StaticResource ProfileTextBox}" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Фамилия">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource ProfilePlaceholder}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=LastNameBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Город -->
            <Grid Margin="0,0,0,20">
                <TextBox x:Name="CityBox" Style="{StaticResource ProfileTextBox}" Text="{Binding City, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Город">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource ProfilePlaceholder}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=CityBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Адрес -->
            <Grid Margin="0,0,0,30">
                <TextBox x:Name="AddressBox" Style="{StaticResource ProfileTextBox}" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Height="60" AcceptsReturn="True" TextWrapping="Wrap"/>
                <TextBlock Text="Адрес">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource ProfilePlaceholder}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=AddressBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Итог -->
            <Border Background="White"
                    BorderBrush="#FFE0E0E0"
                    BorderThickness="1"
                    Padding="25"
                    Margin="0,30,0,0">
                <StackPanel>

                    <!-- Заголовок -->
                    <TextBlock Text="СУММА ЗАКАЗА"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

                    <!-- Стоимость товаров (ItemsTotal) -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Стоимость товаров"
                                   FontSize="14"/>
                        <TextBlock Grid.Column="1"
                                   Text="{Binding OriginalTotal, StringFormat='{}{0:F2} BYN'}"
                                   FontSize="14"
                                   FontWeight="SemiBold"/>
                    </Grid>

                    <!-- Скидка -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Скидка"
                                   FontSize="14"
                                   Foreground="Black"/>
                        <TextBlock Grid.Column="1"
                                   Text="{Binding DiscountSum, StringFormat='- {0:F2} BYN'}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="Black"/>
                    </Grid>

                    <Separator Height="1"
                               Background="#E0E0E0"
                               Margin="0,10,0,15"/>

                    <!-- ИТОГО -->
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Текст "ИТОГО" -->
                        <TextBlock Text="ИТОГО"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   Grid.Column="0" />

                        <!-- Новая цена и старая рядом -->
                        <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
                            <!-- Новая цена -->
                            <TextBlock Text="{Binding TotalSum, StringFormat='{}{0:F2} BYN'}"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="#000000" />

                            <!-- Старая цена перечеркнутая, показывается только если есть скидка -->
                            <TextBlock Text="{Binding OriginalTotal, StringFormat='{}{0:F2} BYN'}"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="Gray"
                                       Margin="10,0,0,0"
                                       TextDecorations="Strikethrough">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasDiscount}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Grid>


                    <!-- Кнопка оформления заказа -->
                    <Button Content="ОФОРМИТЬ ЗАКАЗ"
                            Height="50"
                            Margin="0,20,0,0"
                            Background="Black"
                            Foreground="White"
                            FontSize="16"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CheckoutButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#333333"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#555555"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</Page>