<Page x:Class="Parfuholic.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:models="clr-namespace:Parfuholic.Models"
      Background="#FFF5F5F5">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Perfumes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type models:Perfume}">
                    <Border Width="260" Margin="15" Background="White" CornerRadius="12">
                        <StackPanel>
                            <!-- Image -->
                            <Border Height="220" Background="#FFEDEDED">
                                <Image Source="{Binding Image}" Stretch="UniformToFill"/>
                            </Border>

                            <!-- Info -->
                            <StackPanel Margin="10">
                                <TextBlock Text="{Binding Brand}" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="{Binding AromaGroup}" Foreground="Gray" FontSize="13"/>
                                <TextBlock Text="{Binding Volume}" Foreground="Gray"/>
                                <TextBlock Text="{Binding Price, StringFormat='{}{0} BYN'}" FontSize="18" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>
